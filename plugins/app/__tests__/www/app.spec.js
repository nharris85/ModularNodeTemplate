describe("angular app", function() {
   before(function(){});
   after(function(){});

   context("when some condition", function(){
       beforeEach(function() { });
       afterEach(function() { });
       it('should create angular app', function() {
           expect(true).to.be.true;
       });
   });

});
